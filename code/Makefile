# Default to the modern full-featured build.
#
# The historical "basic" build is still available as Makefile.basic, but the
# default repository build should preserve OpenGL support and the restored image
# backends rather than silently overwriting ../molscript with a reduced binary.

include Makefile.complete
